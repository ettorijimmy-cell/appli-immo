# Copier ce fichier en .env et renseigner les vraies valeurs.
# Ne jamais committer .env — voir CLAUDE.md.

# Scaleway (docs/integrations.md)
DATABASE_URL=postgres://user:password@host:5432/appli_immo
SCW_ACCESS_KEY=
SCW_SECRET_KEY=
SCW_BUCKET_NAME=

# Backend NestJS — authentification
# Obligatoire si NODE_ENV=production (voir auth.module.ts) — le backend
# refuse de démarrer sinon. Repli sur une valeur par défaut en dev
# uniquement.
JWT_SECRET=
JWT_EXPIRES_IN_SECONDS=3600
PORT=3000

# Documents (Module 4) — repli local chiffré temporaire en attendant
# Scaleway Object Storage (voir docs/data-dictionary.md, section documents).
# Défaut si absent : storage/documents relatif au dossier apps/backend.
DOCUMENTS_STORAGE_DIR=

# Desktop (Electron) — origine du backend, aussi utilisée par la CSP
# (electron.vite.config.ts) et l'appel API (lib/api-config.ts).
# Défaut si absent : http://localhost:3000
VITE_API_URL=
